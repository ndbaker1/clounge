{
  "name": "clounge",
  "version": "1.0.0",
  "description": "core data types for clounge",
  "scripts": {
    "format": "prettier --plugin-search-dir . --write ."
  },
  "devDependencies": {
    "prettier": "^2.8.0",
    "typescript": "^4.9.4",
    "monaco-editor": "^0.34.1"
  }
}
